[project]
name = "event-calplot"
version = "0.2.5"
description = "A Python package for visualizing event data as calendar heatmaps."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas==2.3.3",
    "plotly==6.3.1",
]
